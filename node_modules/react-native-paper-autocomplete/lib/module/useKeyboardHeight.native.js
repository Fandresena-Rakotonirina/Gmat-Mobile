import * as React from 'react';
import { Keyboard } from 'react-native';
import { useSharedValue } from 'react-native-reanimated';

function useKeyboardHeight(_) {
  const keyboardHeight = useSharedValue(0);
  React.useEffect(() => {
    const keyboardDidShow = frames => {
      keyboardHeight.value = frames.endCoordinates.height;
    };

    const keyboardDidHide = () => {
      keyboardHeight.value = 0;
    };

    const keyboardDidShowListener = Keyboard.addListener('keyboardDidShow', keyboardDidShow);
    const keyboardDidHideListener = Keyboard.addListener('keyboardDidHide', keyboardDidHide); // cleanup function

    return () => {
      keyboardDidShowListener.remove();
      keyboardDidHideListener.remove();
    };
  }, [keyboardHeight]);
  return keyboardHeight;
}

export default useKeyboardHeight;
//# sourceMappingURL=useKeyboardHeight.native.js.map